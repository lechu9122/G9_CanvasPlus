// Export the refactored components so imports globally still work 
import WidgetGrid from "./widget-grid/WidgetGrid";
import Widget from "./widget-card/Widget";

export default WidgetGrid;
export { Widget };