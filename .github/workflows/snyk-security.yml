name: Snyk Security

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  snyk:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Set up Snyk CLI
        uses: snyk/actions/setup@806182742461562b67788a64410098c9d9b96adb
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Snyk Code (SAST)
        run: |
          snyk code test --sarif-file-output=snyk-code.sarif || echo '{"version":"2.1.0","runs":[]}' > snyk-code.sarif

      - name: Debug SARIF content
        run: cat snyk-code.sarif

      - name: Run Snyk without SARIF (debug)
        run: snyk code test || true

      - name: Snyk Open Source (Dependencies)
        run: snyk monitor --all-projects || true

      - name: Upload Snyk results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: snyk-code.sarif